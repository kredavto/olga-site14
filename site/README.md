# OLGA — цифровой интерьерный showroom

Премиальный сайт мебели по индивидуальному проекту: не каталог, а
интерактивный журнал об интерьерах. Тёмный графит, крупная типографика,
очень крупные кадры, кинематографичный motion.

Опирается на `design-system/` в корне репозитория: та же пропорция
**85 / 10 / 5**, шаг сетки 8px, стиль подписей `uppercase +0.14em`,
акцент — тот же охристо-бронзовый, easing и правила `prefers-reduced-motion`.
Здесь эта система развёрнута в тёмную тему (см. `src/styles/tokens.css`).

---

## Быстрый старт

```bash
cd site
npm run setup     # медиа-плейсхолдеры + локальные шрифты + сборка
npm run dev       # http://localhost:8100
```

Отдельно:

```bash
npm run build     # dist/index.html + dist/assets из content/site.json
npm run media     # перегенерировать плейсхолдеры (python3 + numpy + pillow)
npm run fonts     # обновить локальные подмножества шрифтов
```

`dist/` — готовый статический сайт: заливается на любой хостинг или CDN
как есть. Внешних запросов в рантайме нет (шрифты локальные, карта своя).

---

## Структура

```
site/
├─ content/site.json        ← ВЕСЬ контент: тексты, цены, медиа, фильтры, формы
├─ src/
│  ├─ templates.mjs         шаблоны 16 секций (чистые функции от JSON)
│  ├─ styles/
│  │  ├─ tokens.css         тёмная тема: цвет, шрифты, отступы, движение
│  │  ├─ base.css           сброс, типографика, сетка, header, кнопки
│  │  └─ sections.css       по одной композиции на секцию
│  └─ js/app.js             motion-движок (один rAF-цикл на все scroll-сцены)
├─ tools/
│  ├─ make_media.py         процедурная генерация медиа-плейсхолдеров
│  ├─ fetch_fonts.sh        локализация шрифтов
│  └─ localize_fonts.py
├─ build.mjs                сборка статики (без зависимостей)
└─ dist/                    результат: index.html, assets/, media/
```

---

## Замена контента: всё через `content/site.json`

Структура сайта не зависит от количества элементов. Добавили проект в массив —
он появился в галерее и в фильтрах; добавили материал — вырос мозаичный блок;
добавили этап производства — удлинилась scroll-сцена (её высота считается от
числа слоёв в рантайме). Пересборка — `npm run build`.

| Что меняем | Где | Что происходит |
|---|---|---|
| Проекты | `projects.items[]` | Карточки, фильтры и счётчик пересчитываются |
| Категории | `catalog.items[]` | Новая полноэкранная зона в каталоге |
| Материалы | `materials.items[]` | Плитка мозаики (порядок = раскладка, см. ниже) |
| Отзывы | `testimonials[]` | Слайд в закреплённой сцене |
| Сертификаты | `quality.documents[]` | Карточка + fullscreen-модалка |
| Этапы производства | `production.steps[]` | Слой в scroll-сторителлинге |
| Цены калькулятора | `calculator.steps[].options[]` | `base`, `rate`, коэффициенты `k` |
| Вопросы квиза | `quiz.questions[]` | Шаги и прогресс `01 / 0N` |
| Аннотации в hero | `hero.annotations[]` | Точка `x/y`, подпись `tx/ty`, момент `at` (0…1) |
| Контакты, меню, футер | `contacts`, `nav`, `footer` | Везде сразу |

### Порядок материалов важен
Мозаика в секции «Материалы» задаёт разные форматы по позициям
(1 — вертикальный, 2–3 — широкие, 5–7 — квадраты, 8 — панорама).
Изображения генерируются под эти пропорции — при замене держите тот же
порядок или поправьте `.mats` в `sections.css`.

---

## Медиа

Все изображения в `dist/media/` — **процедурные плейсхолдеры**, чтобы сайт
можно было смотреть и обсуждать до реальной съёмки. Они не претендуют на
роль финального контента: это тёмные архитектурные сцены пяти типов
(общий план, перспектива, фронтальная стена, гардеробная, макро-деталь)
и восемь макро-текстур материалов.

Замена на реальную съёмку — это правка путей в `site.json`, без изменения
разметки. Рекомендуемые размеры — как у текущих файлов
(hero-кадры 1280×720, каталог 1400×1000, проекты wide 1600×1000 /
tall 900×1350 / detail 900×900, case и CTA 1920×1080).

### Фотографии: удалённый URL + локальный запасной кадр

Любое поле с картинкой в `site.json` принимает либо локальный путь
(`media/...`), либо внешний URL. Внешние подключены к CDN Unsplash —
это разрешено их лицензией и бесплатно для коммерческого использования:

```json
"image": "https://images.unsplash.com/photo-XXXX?auto=format&fit=crop&w=1920&h=1080&q=78",
"fallback": "media/case-01.jpg"
```

`fallback` обязателен для внешних адресов: если картинка не загрузилась
(нет сети, адрес протух), `app.js` молча подставит локальный кадр — вместо
фотографии будет заглушка, но не «битая картинка».

**Перед подключением адреса нужно знать, что на снимке.** Ссылка,
которая просто «живая», ещё ничего не гарантирует: в первой версии так на
сайт мебельного ателье попали кадр из ресторана и особняк у воды. Живой
адрес подтверждает доступность, но не тему — сюжет надо проверять
отдельно (открыть снимок, либо посмотреть alt-текст там, откуда взят адрес).

Сейчас подключены два кадра, у которых сюжет подтверждён по alt-тексту
(«Гостиная до/после подготовки»). Остальные слоты держат процедурные
заглушки: адрес с непроверенным сюжетом на боевой сайт не ставится.

**Чтобы добавить фотографию**, достаточно вставить ссылку в `site.json` и
пересобрать (`npm run build`). Подойдёт любая прямая ссылка на изображение —
Unsplash, Pexels или ваш собственный CDN. Параметры Unsplash (`w`, `h`,
`crop`, `q`) позволяют получить нужное кадрирование без графического редактора.

## Типографика

Заголовки — **Playfair Display** (400; `.wordmark` и `.h-3` — 500), контрастная
антиква по референсу `rep4.jpeg`: тонкие волосные, крупный контраст штриха,
каплевидные окончания. Это же значение с самого начала стоит в
`design-system/tokens.json` (`families.display`, источник «rep5, rep3, rep4») —
сайт от собственной дизайн-системы отклонялся, теперь совпадает, включая
трекинг (`scale.hero` −0.02em, `scale.h1` −0.01em).

Токены разделены намеренно:

| токен | гарнитура | что использует |
|---|---|---|
| `--font-display` | Playfair Display | `.h-mega/.h-hero/.h-1/.h-2/.h-3`, `.cat__title`, `.prod__stage h3`, `.wordmark`, крупная цитата |
| `--font-num` | Inter Tight | цифры и служебное: `.num`, `.usp__num`, `.fact__v`, `.step__n`, `.dim__val`, `.calc__sum`, `.brands span`, `.logo__mark`, ссылки мобильного меню |
| `--font-ui` | Inter | основной текст, кнопки, подписи |

Цифры и логотип оставлены на гротеске сознательно: в антикве на мелких кеглях
они теряют читаемость и перестают выглядеть частью интерфейса — сумма в
калькуляторе и значения ползунков читаются как текст, а не как показания.

Встроены только начертания 400 и 500. Ставить заголовку 600 нельзя: браузер
подделает насыщенность (faux bold) и волосные линии загрязнятся — по этой
причине `.wordmark` переведён с 600 на 500.

Антиква шире гротеска, поэтому трекинг пришлось ослабить с −0.035em: с прежним
значением засечки соседних букв смыкались.

## Раскладка карточки проекта

`projects.items[].layout` задаёт форму карточки; без поля раскладки чередуются
по порядку (`'' → mirror → pano → ''`). Поле, а не индекс, потому что при
добавлении проекта в середину списка индексное чередование перетасовало бы
раскладки всех следующих карточек.

| значение | кадры |
|---|---|
| `pano` | панорама 21:9 во всю ширину, под ней широкий кадр (колонки 1–9) и квадрат 1:1 вплотную к нему, обе рамки одной высоты |
| `mirror` | 16:10 справа, 2:3 и 1:1 слева, всё в один ряд |
| без поля | 16:10 слева, 2:3 и 1:1 справа |

В `pano` квадратный кадр выводится примерно в 330 px против 116 px в остальных
раскладках — файл для него нужен соответствующий, иначе снимок будет мягким.

## Слоты под фотографии

Заполняется вставкой ссылки в `content/site.json` по указанному пути, затем `npm run build`.
Локальный кадр из соседнего поля `fallback` остаётся страховкой — трогать его не нужно.

| Путь в `site.json` | Что это на сайте | Запрос для поиска | Размер |
|---|---|---|---|
| ~~`usp.items[0..3].image`~~ | Раздел «Не подгоняем мебель…» | заполнено: rep8→01, rep11→02, rep10→03, rep9→04 | 920×1150 (4:5) |
| `catalog.items[0].image` | Каталог — Кухни | `modern dark kitchen interior` | 1400×1000 |
| `catalog.items[1].image` | Каталог — Шкафы-купе | `sliding wardrobe interior` | 1400×1000 |
| `catalog.items[2].image` | Каталог — Гардеробные | `walk-in closet interior` | 1400×1000 |
| `catalog.items[3].image` | Каталог — Гостиные | `modern living room interior` | 1400×1000 |
| ~~`projects.items[0].images[*]`~~ | Проект 1 «Резиденция в Хамовниках» | заполнено: две ссылки Unsplash + `rep12.jpg` в деталь | 16:10 / 2:3 / 1:1 |
| ~~`projects.items[1].images[*]`~~ | Проект 2 «Квартира на Мосфильмовской» | заполнено: две ссылки Unsplash + `rep13.jpg` в деталь | 16:10 / 2:3 / 1:1 |
| ~~`projects.items[2].images[*]`~~ | Проект 3 «Дом в Барвихе» | заполнено: две ссылки Unsplash + `rep14.jpeg` в деталь | 21:9 / 2:3 / 1:1 |
| `projects.items[3].images[0].src` | Проект 4 — общий план | `japandi interior` | 1600×1000 |
| `projects.items[3].images[1].src` | Проект 4 — вертикальный | `japandi interior vertical` | 900×1350 |
| `projects.items[3].images[2].src` | Проект 4 — деталь | `furniture detail macro` | 900×900 |
| `details.image` | Мебель в деталях (кухня, хотспоты) | `kitchen cabinets close up` | 1800×1150 |
| `production.steps[0].image` | Производство 1 — Материал | `wood veneer texture` | 1500×1000 |
| `production.steps[1].image` | Производство 2 — Раскрой | `cnc woodworking` | 1500×1000 |
| `production.steps[2].image` | Производство 3 — Обработка | `furniture workshop sanding` | 1500×1000 |
| `production.steps[3].image` | Производство 4 — Сборка | `furniture assembly workshop` | 1500×1000 |
| `production.steps[4].image` | Производство 5 — Контроль | `carpenter measuring` | 1500×1000 |
| `production.steps[5].image` | Производство 6 — Монтаж | `furniture installation` | 1500×1000 |
| `testimonials[0].image` | Отзыв 1 — интерьер клиента | `cozy modern interior` | 1500×1100 |
| `testimonials[1].image` | Отзыв 2 — интерьер клиента | `cozy modern interior` | 1500×1100 |
| `testimonials[2].image` | Отзыв 3 — интерьер клиента | `cozy modern interior` | 1500×1100 |
| `materials.items[0].image` | Материал — Натуральный шпон | `wood veneer macro` | см. мозаику |
| `materials.items[1].image` | Материал — Premium matt | `matte surface texture` | см. мозаику |
| `materials.items[2].image` | Материал — Камень | `stone countertop macro` | см. мозаику |
| `materials.items[3].image` | Материал — Металл | `brushed brass texture` | см. мозаику |
| `materials.items[4].image` | Материал — Hidden hardware | `cabinet hinge close up` | см. мозаику |
| `materials.items[5].image` | Материал — Массив дуба | `oak wood grain` | см. мозаику |
| `materials.items[6].image` | Материал — Мрамор | `marble texture` | см. мозаику |
| `materials.items[7].image` | Материал — Текстиль | `fabric texture macro` | см. мозаику |

Подойдёт любая прямая ссылка на изображение. Для Unsplash достаточно взять адрес
вида `https://images.unsplash.com/photo-…` и добавить `?auto=format&fit=crop&w=…&h=…&q=78`.

Карточки раздела «Не подгоняем мебель…» — портретные, `aspect-ratio: 4/5`,
`object-fit: cover`, поэтому файлы кладутся в `dist/media/` уже кадрированными
под 920×1150 (2× от 460 px). Фотографии там подбираются по сюжету подписи, а не
по порядку имён: 01 — эскиз и образцы, 02 — образцы материалов, 03 — цех,
04 — монтаж на объекте.

**Перед вставкой посмотрите на сам снимок.** Живой адрес не гарантирует, что на нём
нужный сюжет — так на сайт уже попадали кадр из ресторана и особняк у воды.

### Hero: видео на первом экране

Сейчас в hero стоит `media/hero.mp4` — перекодированный `rep6.mp4`: без звука,
с ключевым кадром каждые 0.25 с. Это главное требование к файлу: в исходнике
было 2 ключевых кадра на 8 секунд, и перемотка по нему шла рывками.

```bash
ffmpeg -i исходник.mp4 -an -vf "scale=1440:-2:flags=lanczos,unsharp=5:5:0.5:5:5:0.0" \
       -c:v libx264 -preset slow -crf 27 -g 48 -keyint_min 24 -pix_fmt yuv420p \
       -movflags +faststart media/hero.mp4

ffmpeg -i исходник.mp4 -an -vf "scale=854:-2:flags=lanczos,unsharp=5:5:0.5:5:5:0.0" \
       -c:v libx264 -preset slow -crf 28 -g 48 -keyint_min 24 -pix_fmt yuv420p \
       -movflags +faststart media/hero-mobile.mp4
```

**Вес важнее резкости — ролик автозапускается, и пока он не загрузился, на
экране просто ничего не происходит.** Держите десктопный файл в пределах
~1.2 МБ на 8 секунд, мобильный — ~0.5 МБ. Плотные ключевые кадры
(`-g 6 -keyint_min 6 -sc_threshold 0`) нужны **только** режиму `scrub`: они
раздували hero.mp4 до 7.2 МБ, из-за чего на канале 2.5 Мбит/с событие
`canplay` приходило через 23 секунды, а сразу за ним `waiting` — ролик
догонял загрузку и вставал. С обычным GOP тот же материал весит 1.17 МБ при
93% прежней резкости (замер по дисперсии лапласиана) и стартует за 3.7 с.

Проверить вес и время старта:

```bash
ls -l media/hero.mp4                      # ориентир — до ~1.2 МБ
head -c 2000 media/hero.mp4 | strings | grep -m1 'moov\|mdat'   # ждём moov
```

Проверить, что ключевых кадров достаточно:

```bash
ffmpeg -skip_frame nokey -i media/hero.mp4 -vsync 0 -f image2 /tmp/k-%03d.jpg
ls /tmp/k-*.jpg | wc -l     # ожидаем длительность × fps / 6
```

`media/hero-NN.jpg` — те же 40 кадров, нарезанные из видео. Они служат
постером и запасным вариантом; если понадобится вернуть скраб по кадрам
(например, на iOS — там перемотка видео работает хуже), достаточно очистить
`hero.video` в `site.json`.

Поля конфига:

```json
"hero": {
  "video": "media/hero.mp4",
  "videoMobile": "media/hero-mobile.mp4",
  "poster": "https://…",
  "posterFallback": "media/hero-00.jpg",
  "playback": "once",
  "frames": { "pattern": "media/hero-{i}.jpg", "count": 40, "pad": 2 }
}
```

`playback` задаёт режим первого экрана:

| значение | что происходит | высота секции |
|---|---|---|
| `once` (сейчас) | при загрузке видна фотография-постер, через 1.4 с поверх неё проявляется ролик и играет один раз, кадр в конце остаётся | 1 экран |
| `loop` | то же, но ролик идёт по кругу | 1 экран |
| `scrub` | ролик перематывается скроллом | 4.6 экрана |

**Порядок слоёв обратный тому, что кажется логичным: фотография лежит НАД видео**
(`z-index: 2` против `1`) и гаснет по событию `playing`. Видео при этом всегда
непрозрачно. Прятать его через `opacity: 0` нельзя: Safari не запускает
автовоспроизведение невидимого элемента, и получается тупик — не видно, значит
не играет, значит никогда не станет видно. Ролик ставится на паузу, когда экран уходит из вида, и после
окончания не перезапускается.

**Автозапуск нельзя считать гарантированным.** Его запрещают политика браузера,
режим энергосбережения и `prefers-reduced-motion`; Safari к тому же не догружает
медиа заранее, поэтому ждать `canplay` перед `play()` нельзя — событие может не
прийти вовсе. Поэтому запуск устроен так:

1. `src` подключается только через `INTRO` (1.4 с) — до этого элемент пуст, и
   пауза «на фотографию» не съедается; у элемента стоит `autoplay`, поэтому
   запуск делает сам браузер, а `play()` из JS идёт лишь подстраховкой;
2. если он отклонён — над фотографией появляется кнопка «Смотреть ролик»;
3. параллельно ролик стартует от первого же действия пользователя
   (клик, клавиша, касание, колесо);
4. при `prefers-reduced-motion` ролик сам не стартует, но кнопка есть сразу;
5. при ошибке загрузки или недоступном кодеке фотография остаётся на месте и
   появляется та же кнопка;
6. сторож: кнопка показывается принудительно, если загрузка не двигается
   ~8 секунд. `play()` умеет возвращать обещание, которое не разрешается и не
   отклоняется, — тогда не срабатывает ни ветка отказа, ни `error`, и без
   сторожа экран остаётся без ролика и без кнопки. Сторож смотрит на
   `buffered`, а не на часы: на медленном канале ролик стартует поздно, но
   исправно, и мигать там кнопкой незачем.

### `/check` — страница самопроверки

`site/src/check.html` копируется в `dist/check.html` и доступна по адресу
`/check`. Она проигрывает `media/hero.mp4` в отрыве от остальной страницы и
показывает: браузер, состояние `prefers-reduced-motion`, поддержку H.264,
HTTP-ответ на сам файл, пришедшие медиа-события с таймингами, код ошибки,
исход `play()` (включая «обещание висит без ответа») и текущие
`readyState`/`networkState`. Если посетитель говорит «видео не идёт», это
единственный способ узнать, на каком шаге всё обрывается, вместо перебора
гипотез.

### Версионирование ассетов

`app.css` и `app.js` собираются с хэшем содержимого в имени
(`app.09ab8762.css`). Без этого `Cache-Control: max-age=3600` на `/assets/*`
держал у посетителя старый код до часа: правка уезжала на сервер, а в браузере
продолжал работать прежний файл — и починка выглядела как отсутствие починки.
Хэшированные файлы отдаются с `immutable`.

**То же касается медиа, и там срок был год.** `/media/*` отдаётся с
`max-age=31536000, immutable`, а файлы лежат под постоянными именами: замена
фотографии под тем же именем не доходила до вернувшегося посетителя вообще.
Поэтому `build.mjs` на выходе проставляет каждому локальному медиа-адресу
`?v=<8 знаков хэша файла>` — содержимое изменилось, значит изменился и адрес.
Переименовывать сами файлы не нужно, пути в `site.json` остаются читаемыми.

Единственное исключение — `media/hero-{i}.jpg`: в шаблоне кадров стоит
подстановка, файла с таким именем нет, и адрес остаётся без штампа.

Из-за этого первый экран не может молча остаться без видео: либо оно играет,
либо видна кнопка запуска.

Кнопка — сосед `.hero__stage`, а не его потомок: у стейджа свой `z-index: 0`,
внутри него кнопка оказывалась под заголовком и не принимала клик.

`posterFallback` — локальный кадр на случай, если удалённый постер не открылся.

**Аннотации привязаны к таймингу ролика.** `at` — доля длительности, равная
`секунда / длительность` (в режиме `scrub` — та же доля, но по прокрутке); `x`,`y` — точка на экране в процентах, `tx`,`ty` —
подпись. Координаты снимаются с реальных кадров, а не назначаются на глаз:
`object-fit: cover` обрезает видео по бокам, поэтому точки держат подальше
от краёв. Текущая раскладка — 1 c фурнитура, 2 c подсветка, 3 c эмаль,
5 c геометрия, 7.5 c шпон.

**Локальная проверка требует сервера с поддержкой Range.** `python -m http.server`
игнорирует Range-запросы, и видео в браузере не перематывается вообще —
выглядит как поломка скраба, хотя на Vercel всё работает.

### Видео в кейсе проекта

`caseStudy` принимает те же поля, что и hero: `video`, `videoMobile`, `poster`.
Если `video` пуст, секция рендерит `image` — переключение обратно на фотографию
это одно поле.

Ролик автозапускается, когда секция подходит к экрану, поэтому у него те же
требования к весу, что и у hero (рецепт кодирования выше, ~1 МБ на 8 секунд), и
`preload="auto"`: с `preload="metadata"` файл начинал качаться только в момент,
когда посетитель до секции доскроллил, и на канале 2.5 Мбит/с не успевал
стартовать за то время, что секция видна, — а на выходе из вида наблюдатель
ставил его на паузу, и оставался постер.

`rootMargin` наблюдателя — `50%`: ролик стартует за пол-экрана до того, как
секция дошла до глаз. Замер под ограничением скорости: кадр идёт через 1.0 с
после появления секции на 2.5 Мбит/с и через 5.5 с на 1 Мбит/с.

Здесь видео **не скрабится**: у секции своя scroll-анимация (кадр растёт с 70%
до 100% экрана, параллельно считаются цифры), и второй скраб спорил бы с ней.
Ролик просто идёт по кругу и останавливается, когда секция уходит с экрана —
иначе декодер работает вхолостую всю страницу. При `prefers-reduced-motion`
остаётся постер.

Частые ключевые кадры тут не нужны (перемотки нет), поэтому кодируется проще
и легче hero:

```bash
ffmpeg -i исходник.mp4 -an -vf "scale=1920:-2:flags=lanczos,unsharp=5:5:0.9:5:5:0" \
       -c:v libx264 -preset slow -crf 21 -movflags +faststart media/case-01.mp4
```

---

## Подключение CMS / админ-панели

`content/site.json` — это и есть контент-модель. Варианты:

1. **Headless CMS** (Strapi, Directus, Sanity, Netlify CMS). Поля повторяют
   ключи JSON; вебхук на публикацию дёргает `node build.mjs` и деплой.
   Для Netlify CMS достаточно описать коллекции в `admin/config.yml` —
   файл-бэкенд уже совместим по структуре.
2. **Git-based** (Decap/Tina): редактор правит `content/site.json` в репозитории,
   CI собирает `dist/`.
3. **Свой бэкенд**: отдавайте тот же JSON по API и вызывайте `build.mjs`
   в момент публикации; фронт остаётся статикой.

Медиа загружаются в `dist/media/` (или в S3/CDN — тогда в `site.json`
указываются абсолютные URL, код это уже поддерживает).

### Приём заявок
Форма (`src/js/app.js`, обработчик `.sheet__form` submit) сейчас показывает
экран успеха без отправки — это единственная точка интеграции. Подставьте
свой endpoint:

```js
await fetch('/api/lead', { method: 'POST', body: new FormData(form) });
```

Поле `context` уже передаёт, из какого блока пришла заявка
(hero / calculator / quiz / cta-measure / contacts / dock), а из квиза —
выбранные ответы.

---

## Motion

Один `requestAnimationFrame`-цикл обслуживает все scroll-сцены; отдельные
слушатели скролла не плодятся. Реализовано:

scroll-driven video (hero) · pinned horizontal scroll (преимущества) ·
смена пространств (каталог) · parallax (галерея, CTA) · split-text reveal ·
clip-path reveal · staggered reveal (фильтры) · зум кадра + счётчики (case study) ·
масочная смена слоёв (производство) · crossfade-слайды (отзывы) ·
mouse tracking (материалы) · magnetic buttons · кастомный курсор ·
animated number counter (калькулятор) · горизонтальный timeline.

Всё это выключается при `prefers-reduced-motion: reduce` — контент остаётся
полностью доступным (проверено: ни один элемент не остаётся невидимым).

### Две вещи, о которые легко споткнуться
- **`clip-path` схлопывает бокс**, и `IntersectionObserver` перестаёт видеть
  элемент. Поэтому обрезается вложенный `.clip-target`, а наблюдается родитель.
- **`position: absolute` у псевдоэлемента внутри горизонтального скролла**
  цепляется к ближайшему позиционированному предку и растягивает страницу.
  У `.tl__i` поэтому явный `position: relative`.

---

## Доступность и производительность

- Семантические landmark'и, `aria-*` на интерактиве, видимый фокус, skip-link.
- Модалки закрываются по `Esc` и клику по подложке, фокус возвращается.
- Хотспоты и мозаика материалов имеют не-hover альтернативу на тач-устройствах;
  горизонтальные сцены на мобильном становятся вертикальными.
- Шрифты локальные (latin + cyrillic, 16 файлов ≈ 528 КБ), `font-display: swap`,
  первые два preload'ятся.
- Все изображения `loading="lazy"` кроме первого кадра hero (`preload`).
- HTML ≈ 58 КБ, CSS + JS без зависимостей и без внешних запросов.
